# from scripts.captcha import generate_captcha
# from matplotlib import pyplot as plt   
    # if not 'captcha' in st.session_state:
    #     st.session_state['captcha'] = 'show'
    
    # print(st.session_state['captcha'])
    # if st.session_state['captcha'] == 'show':
    #     captcha_image, captcha_text = generate_captcha()
    #     print(captcha_text)
    #     if st.button('Refresh'):
    #         captcha_image, captcha_text = generate_captcha()

    # fig, ax = plt.subplots()
    # plt.imshow(captcha_image)
    # plt.axis('off')
    # st.pyplot(fig)
    # st.text(captcha_text)

       
    # st.session_state['captcha'] = 'check'
    # captcha_user_input = st.text_input('Enter the CAPTCHA')

    # if st.session_state['captcha'] == 'check':
    #     print(captcha_text, captcha_user_input)
    #     if captcha_text != captcha_user_input:
    #         st.session_state['captcha'] = 'show'
        
    #     else:
    #         st.session_state['captcha'] = 'passed'
                